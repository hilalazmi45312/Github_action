jQuery(function($){
    'use strict';

    // Common Elementor Editor Functionality
    function initCommonEditor() {
        // Common editor initialization if needed
        // Senheng Elementor Widgets Editor Initialized
    }

    // Initialize common editor functionality
    if (window.elementorFrontend && window.elementorFrontend.isEditMode()) {
        initCommonEditor();
    }
});
